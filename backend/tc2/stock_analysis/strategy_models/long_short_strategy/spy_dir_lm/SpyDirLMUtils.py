from typing import Dict


def generate_token_map(pct_per_step: float=0.01,
                       min_pct: float=-2.0,
                       max_pct: float=2.0) -> Dict[float, str]:
    """

    :param pct_per_step:
    :param min_pct:
    :param max_pct:
    :return:
    """
    pass